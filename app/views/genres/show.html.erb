<h1><%= @genre.name %></h1>

<h2>Filmes cadastrados deste gênero:</h2>
<ul>
    <% @genre.movies.each do |movie| %>
        <li>
            <%= link_to movie.title, movie_path(movie.id) %>
        </li>
    <% end %>
</ul>